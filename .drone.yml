---
kind: pipeline
type: docker
name: default

clone:
  disable: true

steps:
  - name: clone
    image: docker.io/bdebyl/alpine-git
    commands:
      - git clone https://github.com/bdebyl/docker-sshpass-cron.git .
      - git checkout $DRONE_COMMIT
      - echo -n "1.0.$DRONE_BUILD_NUMBER" > .tags

  - name: publish
    image: plugins/docker
    settings:
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      repo: bdebyl/sshpass-cron
    when:
      branch:
        - main
---
kind: signature
hmac: 0e34dd444cad6fadeb8cf686930b499a7b66f9201183903ab95ff91a78dde5b3

...
